{
  "$schema": "https://opencode.ai/config.json",
  "model": "azure/gpt-5.3-codex",
  "share": "disabled",
  "subagent_depth": 1,
  "permission": {
    "skill": {
      "*": "ask",
      "finance-data-reliability": "allow"
    },
    "external_directory": "ask"
  },
  "agent": {
    "finance-build": {
      "description": "Implement and validate routine PowerShell, Python, Databricks, and Power BI finance workflows",
      "mode": "primary",
      "model": "azure/gpt-5.3-codex",
      "reasoningEffort": "medium",
      "textVerbosity": "low",
      "permission": {
        "edit": "ask",
        "external_directory": "ask",
        "bash": {
          "*": "ask"
        }
      }
    },
    "finance-deep": {
      "description": "Investigate difficult schema drift, reconciliation mismatches, metadata boundaries, and cross-system failures",
      "mode": "primary",
      "model": "azure/gpt-5.3-codex",
      "reasoningEffort": "high",
      "textVerbosity": "low",
      "permission": {
        "edit": "ask",
        "external_directory": "ask",
        "bash": {
          "*": "ask"
        }
      }
    }
  },
  "command": {
    "validate": {
      "description": "Run the repository checks and summarize evidence",
      "agent": "finance-build",
      "template": "Load the finance-data-reliability skill. Run the smallest relevant tests, then the full available suite. Report commands, exit codes, receipts, failures, and anything that was not validated. Do not claim success from inspection alone."
    },
    "diagnose-run": {
      "description": "Classify a failed spool or reconciliation run",
      "agent": "finance-deep",
      "template": "Load the finance-data-reliability skill and its failure taxonomy. Diagnose $ARGUMENTS from receipts and sanitized logs. Separate evidence from inference, classify retryability, and propose the smallest safe next action. Do not modify source data or production systems."
    },
    "pbi-capabilities": {
      "description": "Inventory one Power BI workspace semantic model and choose REST, XMLA, MCP, or Desktop without mutation",
      "agent": "finance-build",
      "template": "Load the finance-data-reliability skill, references/power-bi-boundaries.md, and references/power-bi-premium-workspace-runbook.md. For $ARGUMENTS, perform only the read-only capability inventory using the existing approved authenticated adapter. Never request, print, persist, decode, or accept a bearer token. Return sanitized target identity, observed capacity assignment, REST, XMLA, and MCP capabilities, permission and settings evidence, recommended route, missing prerequisites, and unsupported assumptions. Do not trigger or cancel refresh, refresh permissions, take ownership, rebind a report, change schedules or parameters, commit an MCP transaction, apply service TMDL, call Fabric updateDefinition, save or deploy metadata, or modify any service object."
    }
  }
}
