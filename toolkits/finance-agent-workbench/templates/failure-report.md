# Sanitized failure report

> Do not paste credentials, raw rows, proprietary names, physical server/share paths, request bodies, or screenshots containing work data.

## Classification

- Stage: `DISCOVER | STAGE | PARSE | NORMALIZE | RECONCILE | MODEL_EDIT | VALIDATE | PUBLISH`
- Observed UTC:
- Retryable: `yes | no | unknown`
- Current error class:
- Run or receipt ID alias:

## Runtime

- OpenCode version:
- Provider/model identifier:
- Reasoning profile:
- PowerShell version:
- Python and parser version:
- Power BI Desktop / TE2 / DAX Studio versions, if relevant:
- Network context: `local | VPN | remote share | unknown`

## Sanitized symptom

State what failed and the exact exit code/error category. Replace business names and paths with stable aliases.

## Expected invariant

State the objective condition that should have been true, such as “source fingerprint unchanged during copy” or “no final output before valid XML EOF.”

## Evidence

- Receipt fields:
- Bounded log excerpt:
- Size/hash/count/timestamp facts:
- What has already been ruled out:

Keep evidence separate from interpretation.

## Minimal reproduction

Describe or attach a synthetic fixture that reproduces the behavior without business data.

## Resolution

- Root cause:
- Deterministic remediation:
- New or updated test:
- Documentation/skill change:
- Residual limitation:

